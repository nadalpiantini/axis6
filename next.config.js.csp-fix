// Emergency CSP fix - forces removal of CSP headers
const nextConfig = {
  async headers() {
    return [
      {
        source: '/:path*',
        headers: [
          {
            key: 'Content-Security-Policy',
            value: '' // Empty CSP to override Vercel defaults
          }
        ]
      }
    ]
  }
}

module.exports = nextConfig
